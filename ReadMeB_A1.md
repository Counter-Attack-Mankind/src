1. B->A1 生成器已经从 A1->B 的特殊逻辑中剥离出来，后续只在 `forklift_planner/src/path_generator_routes/b_to_a1/path_generator_route.cpp` 中处理返回 A1 的规则。
2. A1 虚拟点 `id=101` 按 depot endpoint 处理，但不再触发真实货位的 `terminal_reverse` 倒车泊车逻辑；B->A1 的目标是回到 A1，不是进入货架库位。
3. 真实源货位 B 出库仍然固定为倒车出库；倒车出库曲线优先使用 clothoid，如果标准 clothoid 空间不足，则尝试更紧凑的 G2 spiral 兜底，避免退化成“竖直倒车后直接接水平前进”的 90 度 kink。
4. 第一行顶排货位 B0-B9 中，内侧 B2-B7 可以沿原逻辑生成；外侧 B0/B1/B8/B9 不能继续向地图外侧倒车，所以新增了一个通用的“边界源货位反向出库”规则：当目标是 A1 且源货位在第一行外侧列时，规划器会强制先向场地内侧倒车。
5. 对 B0/B1 这类左侧边界货位，车辆先向右倒车，并把倒车落点放到 A1 的右侧；随后在同一位置形成 REVERSE->FORWARD 尖点，再以前进方式从右侧回到 A1。对 B8/B9 这类右侧边界货位则镜像处理：先向左倒到 A1 左侧，再前进回 A1。
6. 这个规则不是只写死 B0/B1/B8/B9 的路径点，而是按“目标为 A1 + 源在第一行外侧列”触发；后续如果 A2 或其他 depot 也遇到同类边界出库问题，可以抽成更通用的 depot-return edge-exit 接口。
7. 针对 B->A1 的第2行和第3行出库换向做了局部修正：第2行源货位回 A1 时，倒车换向点不再贴第一走廊某一条车道线，而放在第一走廊两条车道之间，给倒车转弯和前进转向都留出余量；第3行源货位回 A1 时，出库落点以及后续第二走廊到第一走廊的过渡都使用第二走廊第二车道，避免骨架先把车拉到第一车道再进行过渡，从而减少极限倒车弯和前进弯。
8. 本次继续修正第2行 B->A1：日志显示骨架从中间换向点又被拉回第一走廊第二车道，形成 `(换向点)->(第二车道)->A1` 的直角段，所以现在第2行回 A1 的倒车换向点直接落到第一走廊第一车道，并把后续 `goal_lane_y` 锁定在这个换向高度，后面的前进段直接沿第一车道直线和黄色弧线进入 A1，不再回落到第二车道。
9. 本次继续修正第2行 B->A1 的出库前缀：日志里的粗骨架已经在第一走廊第一车道，但 kink 仍出现在库口后轴点附近，说明问题不是主路径车道选择，而是倒车出库曲线仍按 `dock_x/dock_y` 作为几何起点，和实际输出的后轴参考点错位；现在第2行回 A1 的初始倒车曲线改为从真实后轴点拟合，避免曲线从后轴后方开始，从而减少库口处伪造的 REVERSE->FORWARD 直角换向。
10. 本次继续收紧第2行 B->A1 的出库曲线判据：原先普通出库允许 `straight_len >= -sample_ds` 的采样容差，但第2行回 A1 时这会放过“曲线起点落在后轴点后方”的候选，导致库口附近仍出现假的 REVERSE->FORWARD kink；现在仅对第2行回 A1 要求 `straight_len >= 0`，并在 debug 日志中输出 `reverse_origin`，用于确认曲线起点是否真正位于后轴参考点之后。
11. 本次继续修正第2行 B->A1 的出库曲线模型：第2行回 A1 不再优先使用通用 clothoid 生成倒车出库段，而是直接使用较短、较可控的 G2 spiral，把车辆从库位倒车圆弧引导到第一走廊第一车道，再由后续主路径沿固定黄色车道线返回 A1；其他行仍保留原来的 clothoid/G2 兜底顺序，避免误伤已经可用的路线。
12. 本次继续修正第2行 B->A1 的出库前缀姿态：日志显示几何点仍沿竖直方向倒车，但曲线内部 heading 已经把路径点姿态提前置为 0 度，导致 validator 在库口附近判定车身从 -90 度瞬间跳到 0 度；现在第2行回 A1 的倒车前缀不再使用曲线自带 heading 填 `theta`，而是根据相邻采样点的几何 chord 反算运动方向，再按 REVERSE 得到车身姿态，使竖直出库、倒车圆弧、最终尖点接车道线的姿态连续。
13. 本次为第2行 B->A1 增加强制诊断日志：因为运行日志中仍未出现上一轮新增的 `chord reverse prefix`，无法确认当前二进制是否进入了第2行专用前缀分支，所以现在在 `prepend_initial_reverse()` 入口增加 `prepend enter` 日志，并在 chord 前缀生成后输出关键前缀点，后续只要重新编译并运行到这份代码，就能直接判断问题是“未进入专用分支/未重新编译”，还是“专用前缀内部某个点的 theta 或 type 仍不合法”。
14. 本次确认当前运行日志仍未出现 `prepend enter` 或 `chord reverse prefix`，说明运行节点没有包含上一轮新增的前缀诊断代码；因此先停止继续盲调几何，并在最早的 `route=` 调试行加入 `debug_rev=prepend_chord_v1` 版本标记，后续只要重新编译并重启节点，日志中应首先出现该标记，若没有则说明仍在运行旧二进制或 launch 未切到最新节点。
15. 本次定位到第2行 B->A1 仍然报 kink 的关键原因不是车道几何继续错误，而是回旋曲线不可行时的通用兜底分支仍把 A1 当作 endpoint，调用 `reverse_gen.generate(src, tgt, ...)` 后直接返回了一条通用 reverse 路径，绕过了 B->A1 专用的 `prepend_initial_reverse()`，所以日志里能看到 `debug_rev=prepend_chord_v1`，却始终看不到 `prepend enter`；现在已禁止 A1 depot 走这个真实货位 reverse 兜底，后续第2行回 A1 必须继续留在 B->A1 专用生成器内处理出库前缀和黄线回库。
16. 本次确认 `forklift_map/src/map_node.cpp` 中的黄色车道线只是 RViz marker，可视化时用 `forklift_geom::fit_clothoid_turn()` 画出“第一走廊上侧车道到 A1 中脊竖线”的 clothoid，但规划器并不会直接读取这些 marker；B10->A1 的骨架已经落在这条黄线上，失败点是 B->A1 生成器内部仍用 `build_clothoid_turn()` 先生成固定 ramp 再检查切线长度，导致 `0.231m` 的 A1 竖直段被判 `limit=0.228` 不可行并退到 arc fallback，所以现在把 B->A1 的角点拟合改成同黄线一致的 `fit_clothoid_turn()`，让规划器按地图黄线的自适应 clothoid 通过，而不是退化成曲率不连续的 arc。
17. 本次继续对齐 `map_node.cpp` 的黄线逻辑：A1 depot 不是普通货位末端入库，最后一段实际是在第一走廊上侧车道接入 A1/顶部出口黄线，因此不能再用普通终端角点的 `0.99 * next_len` 保护把竖直段从 `0.231m` 砍到 `0.228m`；现在仅对 A1 depot 的末端角点放宽为使用完整竖直段，让 `fit_clothoid_turn()` 有机会复现地图黄线的曲率连续回旋线，而不是因 1% 保护量掉入 arc fallback。
18. 本次按“不是参考黄线、而是直接走同一条车道线”的要求处理第2行 B->A1：对于源货位在第2行且目标为 A1 depot 的路径，末端不再交给通用 `simplified` 角点平滑，而是直接用 `map_node.cpp` 顶部出口黄线同一套参数生成 `corridor1` 上侧车道水平直线、A1 中脊处 `fit_clothoid_turn(kPi/2, ..., mt_port=0.30)` 回旋线、以及顶部竖线，随后再接已有的倒车出库前缀；这样 B10->A1 的最后进 A1 段与地图黄色车道线几何一致，不再因为 A1 虚拟终点过短而进入 arc fallback。
19. 本次补全第2行 B->A1 精确黄线段的左右来车方向：上一条只固定了左侧来车 `din=+1`，现在按倒车出库后所在的 A1 左/右侧自动选择 `din=+1/-1`，并使用与 `map_node.cpp::addClothoidTurn()` 相同的局部曲线旋转公式生成世界坐标；因此 B10 这类左侧货位和右侧第2行货位都会走各自对应的同一条黄色车道转弯线。
20. 本次诊断发现最新终端日志仍显示 `debug_rev=prepend_chord_v1` 且继续进入 `clothoid infeasible` / `arc fallback`，说明运行中的节点还没有包含第18、19条新增的精确 A1 黄线段代码；现在把首行调试版本标记改成 `debug_rev=exact_a1_lane_v2`，后续重新编译并重启后应同时看到该版本标记和 `exact A1 lane` 日志，若仍看到旧标记则说明运行的是旧二进制或 launch 没切到当前构建产物。
21. 本次先处理第3行货位中 17/19 这类右侧列 B->A1 的货架碰撞：日志显示它们被选到右侧过渡竖线 `x=2.155`，随后在 `y=3.791` 横向并线到 A1 中脊时车身扫到中间货架 `[x 0.580..1.720, y 3.129..3.611]`；现在仅对第3行下排回 A1 且 `src.col >= 3` 的情况强制使用左侧过渡竖线 `row1_left_up_x`，让 17/19 复用 11/13/15 已经成功的左侧内侧车道策略，避免右侧横向并线扫货架。
22. 本次修正第3行右侧货位 B17/B19 回 A1 的过渡车道选择：上一轮为了避开右侧扫货架临时把 `src.row_id == 2 && src.col >= 3` 强制切到左侧，但这不符合“先走第二走廊第二车道线，再拼接右侧竖向车道线”的路径定义；现在该分支改为锁定右侧内侧竖向线 `row1_right_down_x`，并让第一过渡点也使用同一个 `row1_right_down_x`，避免通用 2->1 上行逻辑误选右外侧 `row1_right_up_x`，同时让右侧竖向连通线长度和左侧内侧车道策略保持一致。
23. 本次继续修正第3行右侧 B17/B19 走右内侧车道后在第一走廊进 A1 处的曲率不连续：日志显示主体骨干已经正确落到 `row1_right_down_x`，但右内侧竖向线到 A1 中线的横向距离只有约 `0.615m`，通用 `fit_clothoid_turn()` 在 `j=2` 处因切线余量不足失败并退到 `arc fallback`，最终被 catalog 判为 `curvature_discontinuity`；现在仅对 `src.row_id == 2 && src.col >= 3 && target=A1` 的右内侧分支增加紧凑 G2 spiral 兜底，保持右内侧路径不变、地图参数不变，同时避免该紧凑角点退化成曲率不连续圆弧。
24. 本次按“不要第一走廊横向线段、不要让车身姿态转到 180 度”的要求重做 B17/B19 回 A1 末端策略：撤掉上一轮针对紧凑角点的 G2 硬拐兜底，改为仅对第3行右侧回 A1 分支直接生成专用路径，先用正常 clothoid 从第二走廊接入右内侧竖线 `row1_right_down_x`，过第1行货架上沿 `y7()+0.04` 后沿竖向同向 lane shift 慢慢并到 A1 中线，再沿 A1 竖线进库；这样不再生成 `(row1_right_down_x, goal_lane_y) -> (A1_x, goal_lane_y)` 的水平段，车辆姿态保持接近向上行驶，更适合跟踪。
25. 本次按“回滚到和左侧同个逻辑”的要求撤掉 B17/B19 专用竖向并线直入 A1 策略，不再提前返回 `build_right_inner_direct_a1_path()`，而是让第3行右侧回 A1 重新进入通用 skeleton/turn planner；为了让右侧具备和左侧 B15 相同的横向转弯余量，右侧 staging 竖线使用 `2 * A1_x - row1_left_up_x` 的镜像位置，当前约为 `x=2.065`，这样从 staging 线到 A1 中线的横向距离与左侧 `row1_left_up_x -> A1_x` 同为约 `0.815m`，避免 `row1_right_down_x=1.865` 只有 `0.615m` 时触发 arc fallback 或紧凑硬拐。
26. 本次按用户明确指定回滚到第22条语义：撤销上一轮 `right_stage_skeleton_v5` 的镜像 staging 方案，B17/B19 回 A1 时第一过渡点重新锁定到右侧内侧竖向车道 `row1_right_down_x`，即保持“第二走廊第二车道线 -> 右侧内侧竖向连通线 -> A1”的骨干；当前调试标记更新为 `debug_rev=right_inner_lane_v2`，不再使用 `row1_right_a1_stage_x`。
