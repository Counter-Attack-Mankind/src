# AGENTS.md

## 1. 文档目的与适用范围

本文件是本仓库的项目级开发与 AI 协作约束，适用于仓库根目录及其全部子目录。

所有开发者、自动化工具和 AI 代理在分析、修改、测试或记录本项目时，都必须遵守本文件。若下级目录存在更具体的 `AGENTS.md`，下级规则可以补充本文件，但不得降低本文件规定的安全要求、证据要求和验证要求。

本文件建立时的正式项目基线为：

- 当前仿真主链：`forklift_map`、`forklift_planner`。
- 公共 ROS 消息接口：`experiment/sandbox_msgs`。虽然位于 `experiment`，但它是核心规划包的直接依赖，作为公共接口包例外处理。
- 实车适配实验模块：`experiment` 下除 `sandbox_msgs` 外的定位、控制器、底盘和 VRPN 相关包。
- 正式业务流程：`B -> A1 -> B`，即车辆从 B 库位前往 A1，完成取货等待后，再从 A1 前往目标 B 库位完成卸货。
- A2 是下一阶段业务站点，当前不属于已实现业务流程。

来源：

- `forklift_planner/src/multi_vehicle_patrol_node.cpp`：`MultiVehiclePatrolNode`、`updateDwellAndTasks()`、`handleLegArrival()`。
- `forklift_planner/include/forklift_planner/multi_vehicle/vehicle_agent.h`：`VehicleMode`、`MissionPhase`、`VehicleAgent`。
- `forklift_planner/src/multi_vehicle/task_allocator.cpp`：`TaskAllocator::assignPickupLeg()`、`prepareDropoffLeg()`、`activatePreparedDropoffLeg()`、`assignNextTask()`。
- `forklift_planner/package.xml`：对 `forklift_map` 和 `sandbox_msgs` 的直接依赖。

## 2. 事实与证据规则

### 2.1 禁止推测功能

所有技术结论必须来自当前仓库中的实际代码、配置、构建文件、launch 文件、消息定义、测试或运行证据。

禁止仅根据以下信息推断功能已经存在：

- 项目名、包名、目录名或文件名；
- 注释中的计划、阶段名称或历史描述；
- 自动驾驶、机器人或 ROS 领域的通用经验；
- 已废弃代码、被注释代码或默认关闭的策略；
- 单次聊天记录中没有落实到代码和正式文档的设想。

无法从现有证据确认时，必须明确写出：

> 未知，需要确认

不得使用“应该”“大概”“通常如此”等措辞代替确认。

### 2.2 重要结论必须可追溯

架构、接口、状态转换、规则优先级、安全行为和参数作用等重要结论，应尽量注明：

- 来源文件；
- 类或结构体；
- 函数；
- 参数名或 topic 名；
- 必要时注明对应实验编号或 Git commit。

代码与文档发生冲突时，不得静默选择一方。必须先报告冲突，确认真实基线后再修改代码或文档。

### 2.3 区分代码存在、默认启用和正式支持

文档和评审必须区分以下状态：

- 已实现且属于正式方案；
- 已实现但仅用于调试或实验；
- 部分实现；
- 默认关闭；
- deprecated；
- 下一阶段规划；
- 未知，需要确认。

代码存在不等于正式支持，能够编译不等于通过验证，存在安全判断不等于已经完成实车安全认证。

## 3. 正式架构边界

### 3.1 当前仿真主链

`forklift_map` 负责静态仓库地图、货架、货位、道路几何以及 RViz 地图显示。

`forklift_planner` 负责路径生成、路径验证、任务分配、多车协调、仿真状态推进、实车协调输出和规划相关可视化。

正式业务状态机以以下代码为准：

- `VehicleMode`：`NEED_TASK`、`ACTIVE`、`DWELL`；
- `MissionPhase`：`TO_A1`、`PICKUP_DWELL`、`WAIT_DROPOFF_TASK`、`TO_B`、`UNLOAD_DWELL`；
- 正式循环：`TO_A1 -> PICKUP_DWELL -> TO_B -> UNLOAD_DWELL -> TO_A1`；
- `WAIT_DROPOFF_TASK` 是 A1 后续 B 任务暂时无法分配时的等待阶段。

### 3.2 公共接口包例外

`experiment/sandbox_msgs` 是公共 ROS 消息接口包，不按普通实验代码处理。修改其中以下消息会影响核心规划链和实车适配链：

- `AprilObject.msg`；
- `Trajectory.msg`；
- `TrajectoryPoint.msg`；
- `ChassisCommand.msg`。

修改消息字段、单位、语义或常量前，必须检查所有发布者、订阅者、`forklift_planner/package.xml` 和相关 CMake 依赖，并执行接口回归验证。

### 3.3 实车适配实验模块

`experiment` 下除 `sandbox_msgs` 外的包定位为实车适配实验模块，不得描述为当前仿真主链。

当前虚拟机环境缺少实车实验依赖，因此 `experiment/chassis` 和 `experiment/vrpn_client_ros` 中的 `CATKIN_IGNORE` 属于已确认的环境隔离措施。不得为了让当前虚拟机全量编译而擅自删除这些文件。

实车适配代码的存在不代表当前环境可以构建或运行实车链。涉及实车链的完成结论必须有对应环境中的构建和运行证据；否则标记“未知，需要确认”。

### 3.4 A2 边界

A2 是下一阶段业务站点。当前仓库中存在 A2 方向的路径生成和调试代码，但不得据此描述为已实现 A2 业务流程。

在负责人确认 A2 的任务语义、状态机、站点定义、接口和验收标准前，禁止把 A2 接入正式任务分配或多车协调主流程。

## 4. Deprecated 逻辑管理

以下逻辑已确认为 deprecated，不属于正式设计方案：

- 普通 `B -> B` 业务流程；
- `simple_forward_demo`；
- 粗粒度资源仲裁；
- 旧死锁处理策略；
- 通过“从当前位置重新规划到其他库位”解决死锁的逻辑。

对 deprecated 代码执行“保留但冻结”规则：

- 当前阶段不得删除；
- 禁止增加新功能；
- 禁止将其作为新方案继续扩展；
- 禁止让正式 `B -> A1 -> B` 流程新增对它的依赖；
- 禁止通过默认参数、launch 覆盖或隐式回退重新启用；
- 如因编译、接口兼容或严重缺陷必须修改，只允许最小范围修复，并须事先获得项目负责人确认；
- 修改时必须标注 deprecated 范围，避免把旧逻辑误写为当前架构。

特别约束：后续死锁设计禁止采用“当前位置重新规划到其他库位”作为新策略。相关现存代码只能按 deprecated 逻辑维护。

来源：

- `forklift_planner/config/planner_param.yaml`：`simple_forward_demo` 以及旧策略开关。
- `forklift_planner/src/multi_vehicle/rule_engine.cpp`：`RuleEngine::decide()` 中停用的粗粒度资源仲裁和可选旧 cycle break。
- `forklift_planner/src/multi_vehicle/task_allocator.cpp`：`TaskAllocator::replanFromPose()`。
- `forklift_planner/src/multi_vehicle_patrol_node.cpp`：普通模式与死锁恢复的现存兼容逻辑。

## 5. 安全强制规则

安全规则优先于任务吞吐量、路径长度、仿真效果、日志简洁度和临时实验便利性。

### 5.1 禁止关闭安全保护

未经项目负责人明确批准，禁止关闭、绕过、削弱或将默认值改为关闭以下保护：

- 仿真或实车中的车辆碰撞硬保护；
- 实车全局急停；
- 动捕超时停车；
- 实车基于真实足迹的紧急碰撞保护；
- 路径边界检查；
- 正式流程使用的路径尖点/非法换向检查；
- 到达、停车和任务切换过程中的停止约束。

不得以“仅用于调试”为理由让可能控制实车的 launch、配置或代码默认绕过安全保护。

### 5.2 安全阈值修改

修改以下类型参数前，必须建立实验记录并取得项目负责人批准：

- 车体尺寸、轴距、前后悬和规划参考点；
- 最大速度、加速度、减速度、转角和转角速度；
- 碰撞余量、实车紧急余量和跟车距离；
- 动捕超时、到达容差和急停相关参数；
- 冲突预测时间、动作保持时间和停车距离；
- 路径边界、货架碰撞、曲率连续性和尖点过滤开关或阈值。

实验记录至少包含原值、新值、修改原因、适用模式、测试场景、日志、结果、结论、Git commit 和是否保留。

### 5.3 安全代码修改

修改以下类或函数前，必须先分析完整调用链和规则叠加顺序：

- `RuleEngine` 及其 `decide()`、冲突检测、跟车、目标占用、前向净空、优先级和动作应用逻辑；
- `MultiVehiclePatrolNode::advanceVehicles()`、`realAdvance()`、`realHardGuard()`、`publishRealOutputs()`、急停与动捕回调；
- `TaskAllocator::validatePath()`、`validatePose()`；
- footprint/OBB 构造与重叠检测；
- 控制器和底盘命令输出。

禁止只修复单一冲突场景而不检查其对其他规则、A1 离场、跟车、等待图和安全硬约束的影响。

### 5.4 未确认的安全能力

如果代码、launch 注释和实际接线不一致，必须标记“未知，需要确认”，不得宣称保护已经生效。

例如，参数仅在 launch 中声明但没有被节点读取时，不得据此声称对应看门狗或控制策略已实现。

## 6. 路径规划开发约束

路径规划的正式输入输出和行为必须以 `PathGenerator`、路线分派代码、`TaskAllocator` 的路径验证代码为准。

修改路径规划时必须遵守：

- 禁止只修改单个方向后假定反向路径自动正确；
- 必须分别检查 `B -> A1` 和 `A1 -> B`；
- A2 路径代码只能作为下一阶段能力维护，不得自动接入正式业务；
- 修改 Clothoid、lane shift、arc fallback、路径拼接或 reverse docking 后，必须检查位置连续性、航向连续性、曲率退化、换向 cusp、边界和车身足迹；
- 禁止用删除路径拒绝条件的方式掩盖路径生成缺陷；
- 禁止把 arc fallback 描述为 Clothoid 成功；
- 禁止修改路径生成逻辑后不执行回归验证；
- 路径参数应保持单一真值源。发现 `forklift_map` 与 `forklift_planner` 存在重复或冲突参数时，先报告，不得擅自选择。

路径规划修改至少需要执行与变更范围相匹配的路径目录诊断，覆盖有效路径、空路径、边界、尖点、倒车段、fallback 使用情况和相关 A1 路径。

## 7. 多车协调开发约束

当前正式协调逻辑的权威入口是 `RuleEngine::decide()`。修改前必须核对实际执行顺序，不得仅阅读函数名或历史注释。

禁止事项：

- 未分析规则冲突和叠加顺序直接修改 `RuleEngine`；
- 建立成对但不具传递性的优先级规则，却把它当作全序使用；
- 重新启用粗粒度资源仲裁或旧死锁策略；
- 使用 deprecated 的当前位置改道方法作为新死锁方案；
- 为提高吞吐量绕过前向净空、目标库位占用或碰撞硬保护；
- 只验证车辆能移动，不验证是否碰撞、长期等待、动作抖动或任务饥饿；
- 删除冲突位置、blocker、reason、wait time、deadlock cluster 等关键诊断信息。

修改优先级、跟车、冲突检测、A1 离场、目标占用、动作保持或等待图后，必须执行多车 batch 回归，并保留可复现的随机种子、参数和结果。

## 8. 确定性与实验可复现性

当前 Git 修改状态属于正式基线的一部分：项目已新增任务指派的确定性真伪随机逻辑，用于保证任务分配结果可复现。

任何涉及任务选择、候选集顺序、随机数发生器或种子的修改，都必须：

- 保留显式随机种子；
- 说明是否要求跨运行复现；
- 避免依赖未定义的容器遍历顺序；
- 在实验日志中记录随机种子和相关参数；
- 使用相同输入重复运行并比较任务序列；
- 不得以系统随机源覆盖正式可复现实验，除非实验目的明确要求且已记录。

不得擅自恢复、覆盖或清理审计时已存在的 Git 修改。任何与当前任务无关的用户修改都必须保留。

## 9. 回归验证要求

### 9.1 必须回归的变更

以下修改必须执行回归验证：

- `PathGenerator`、路线分派、Clothoid 或 terminal docking；
- `TaskAllocator`、任务缓存、随机任务选择或 A1 航段分配；
- `RuleEngine`、footprint、`TrafficResourceMap` 或车辆状态推进；
- 地图尺寸、货位、道路、车辆几何或安全参数；
- ROS 消息定义、topic、单位或 frame；
- 仿真/实车共享逻辑。

### 9.2 最低验证内容

核心路径逻辑修改至少需要：

- 构建相关包；
- 运行路径目录诊断；
- 覆盖 `B -> A1` 与 `A1 -> B`；
- 检查空路径、非法尖点、倒车 cusp、边界和 fallback 统计；
- 保存参数、命令和结果。

核心协调逻辑修改至少需要：

- 构建相关包；
- 运行确定性多车 batch 回归；
- 使用固定随机种子重复验证；
- 检查碰撞、死锁、长期等待、任务完成数和异常退出；
- 对受影响的典型场景进行 RViz 或等价轨迹核对；
- 保存日志并建立实验编号。

如果当前环境无法执行必要测试，必须明确说明未执行项、原因和风险。不得把“编译通过”写成“回归通过”。

当前仓库缺少完整自动化测试体系。在正式测试补齐前，现有诊断程序和 batch 运行只能作为回归手段，不能被描述为完整测试覆盖。

## 10. ROS、C++ 与配置规范

### 10.1 ROS 接口

- topic、消息字段、单位和 frame 是接口契约，修改必须同步检查全部发布者和订阅者；
- 不得把毫米和米混用；若接口保持毫米，必须在边界处显式转换并在文档中记录；
- 不得仅修改 launch 参数而不确认节点实际读取；
- 新增参数必须定义默认值、命名空间、单位、适用模式、读取位置和验证方法；
- 不得依赖未在 `package.xml` 或构建文件中声明的核心运行依赖；实车实验依赖例外必须明确记录环境前提。

### 10.2 C++

- 保持项目当前 C++17 构建基线；
- 优先使用明确所有权和现有类型，不引入隐式全局状态；
- 数值常量应来自参数或有清晰的物理含义和单位；
- 状态转换必须集中、可追踪，并更新 `reason` 或等价诊断字段；
- 安全判断不得依赖未初始化数据、隐式单位或未检查的车辆 ID；
- 不得通过大段复制形成新的路线或规则实现，除非负责人确认其维护策略。

### 10.3 配置

- 修改实验参数必须记录；
- 禁止在 YAML、launch 和源码默认值中维护互相冲突的同一参数；
- 参数从 YAML 加载不代表节点实际生效，必须核对读取命名空间；
- 禁止把临时现场调参直接作为新默认值而没有实验结论和负责人批准。

## 11. 日志与实验记录

禁止删除或弱化以下关键日志：

- 路径生成失败和拒绝原因；
- fallback、kink、边界和货架碰撞诊断；
- 每车任务、状态、动作、速度、`path_s`、等待时间、blocker 和 reason；
- 冲突位置、冲突车辆、优先级结果；
- 动捕失联、急停和实车硬保护；
- 死锁等待图、环成员、冲突几何和现场快照；
- batch 汇总和异常退出信息。

每次参数实验或核心逻辑实验必须分配唯一编号，例如 `EXP-001`，并记录：

- Git commit 和工作区状态；
- 修改文件、类和函数；
- 实验目的；
- 参数变化；
- launch/命令、车辆数和随机种子；
- 观察日志；
- 结果与结论；
- 是否保留；
- 产物路径。

不得依赖会被下一次运行覆盖或位于临时目录中的日志作为唯一实验记录。

## 12. AI 协作规则

AI 代理必须：

- 先阅读本文件，再执行仓库任务；
- 在修改前定位真实调用链、参数读取点和接口消费者；
- 区分正式流程、实验模块、公共接口例外和 deprecated 代码；
- 保护用户已有未提交修改，不得擅自 reset、checkout、覆盖或删除；
- 修改尽量最小化，禁止顺手重构无关代码；
- 使用实际代码证据解释结论，并尽量注明文件、类和函数；
- 发现不确定信息时停止推断，标记“未知，需要确认”，并在影响实现方向、安全规则、正式边界或废弃范围时提前询问负责人；
- 完成代码修改后执行与风险相称的构建和回归验证；
- 明确报告未执行的验证以及原因；
- 同步更新受影响的正式文档和实验记录。

AI 代理禁止：

- 根据聊天印象创造代码中不存在的功能；
- 把 `experiment` 整体描述为当前仿真主链；
- 把 A2 描述为当前已实现业务流程；
- 将普通 `B -> B` 或 `simple_forward_demo` 作为正式方案继续开发；
- 重新启用或扩展 deprecated 资源仲裁、死锁策略和当前位置改道逻辑；
- 未经批准关闭安全保护或修改安全阈值；
- 未执行回归就声称路径或多车协调修改完成；
- 删除关键日志以减少输出；
- 修改实验参数但不留下记录；
- 把运行时未知事项写成确定事实。

## 13. 文档维护规则

正式文档必须与当前代码同步，并遵守以下要求：

- 架构和流程文档使用正式 `B -> A1 -> B` 基线；
- deprecated 代码必须明确标识，不能与正式方案并列而不说明状态；
- A2 只能标记为下一阶段业务站点；
- 实车能力必须说明环境依赖和验证状态；
- 参数文档必须区分“已配置”“已读取”“已使用”“已验证”；
- 安全能力必须区分“代码保护存在”和“实车验证完成”；
- 重大设计变化必须记录原因、候选方案、影响和验证证据；
- 文档中无法确认的内容统一标记“未知，需要确认”。

修改代码却不更新已失效文档，或修改文档却不核对实际代码，均视为未完成工作。
